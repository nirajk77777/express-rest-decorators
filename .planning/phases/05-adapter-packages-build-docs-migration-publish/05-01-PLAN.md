---
phase: 05-adapter-packages-build-docs-migration-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DI-03]

must_haves:
  truths:
    - "REQUIREMENTS.md DI-03 wording reflects docs-only useContainer recipe (no separate adapter package)"
    - "ROADMAP.md Phase 5 SC #1 reflects single-package repo (not monorepo / pnpm workspaces / packages/core+typedi)"
    - "ROADMAP.md Phase 5 SC #4 reflects docs-only DI recipe (not separate @scope/express-controllers-typedi adapter package)"
    - "ROADMAP.md Phase 5 Plans subsection lists 05-01..05-07 filenames (not the copy-pasted 04-01..04-06)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DI-03 line rewritten to docs-only framing"
      contains: "DI-03"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 SC #1, SC #4, and Plans block updated to single-package + docs-only + correct filenames"
      contains: "Phase 5"
  key_links:
    - from: ".planning/REQUIREMENTS.md DI-03"
      to: "CLAUDE.md Direction Override (2026-05-08)"
      via: "single-package + docs-only DI recipe"
      pattern: "useContainer"
    - from: ".planning/ROADMAP.md Phase 5 SC #1"
      to: "BUILD-06 single-package lock"
      via: "no monorepo / no pnpm workspaces / no packages/*"
      pattern: "single-package"
---

<objective>
Reconcile REQUIREMENTS.md and ROADMAP.md with the locked single-package + docs-only-DI direction (CLAUDE.md Direction Override 2026-05-08, CONTEXT.md D-01, BUILD-06) before any code changes land. Three doc surgeries:

1. REQUIREMENTS.md DI-03 — reword from "separate adapter package published" to "TypeDI is documented as a useContainer recipe in README; no separate adapter package".
2. ROADMAP.md Phase 5 SC #1 — replace monorepo/pnpm-workspaces/packages-core+typedi wording with single-package wording.
3. ROADMAP.md Phase 5 SC #4 — replace "separate @scope/express-controllers-typedi adapter package is published alongside core" with the docs-only recipe wording.
4. ROADMAP.md Phase 5 "Plans" subsection — currently lists `04-01-PLAN.md` … `04-06-PLAN.md` (copy-paste typo); replace with the seven new `05-NN-PLAN.md` filenames + brief objectives that match this phase's plan set.

Purpose: every downstream plan (and any cross-AI reviewer) must read source docs that match the locked direction. Leaving the stale wording in place creates phantom requirements (a TypeDI sibling package that never ships).
Output: edited REQUIREMENTS.md and ROADMAP.md committed under a single doc-rewrite commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-adapter-packages-build-docs-migration-publish/05-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword REQUIREMENTS.md DI-03</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file; locate line beginning `- [ ] **DI-03**:`)
    - .planning/phases/05-adapter-packages-build-docs-migration-publish/05-CONTEXT.md §"spec_lock" + §"D-01"
    - CLAUDE.md §"Direction Override (2026-05-08)" + §"Constraints"
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Locate the DI-03 line in the `### DI Hook (DI)` section. Current text:

    `- [ ] **DI-03**: A separate \`@scope/express-controllers-typedi\` adapter package is published alongside core (TypeDI 0.x reference adapter)`

    Replace with EXACTLY:

    `- [ ] **DI-03**: README documents a \`useContainer\` recipe wiring TypeDI (and any \`.get(token)\`-shaped container) into the core \`useContainer(IocAdapter)\` hook from Phase 1; no separate \`@scope/express-controllers-typedi\` adapter package is published — the single-package rule (BUILD-06) precludes a sibling package.`

    Do NOT change the `- [ ]` checkbox state; do NOT touch any other DI-XX requirement; do NOT touch the Traceability table (DI-03 still maps to Phase 5).
  </action>
  <verify>
    <automated>grep -F "README documents a \`useContainer\` recipe wiring TypeDI" .planning/REQUIREMENTS.md && ! grep -F "A separate \`@scope/express-controllers-typedi\` adapter package is published alongside core" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DI-03" .planning/REQUIREMENTS.md` returns exactly 2 (one in the requirements list, one in the Traceability table) — same as before the edit.
    - `grep -F "useContainer" .planning/REQUIREMENTS.md` matches the new DI-03 line.
    - `grep -F "separate \`@scope/express-controllers-typedi\` adapter package is published alongside core" .planning/REQUIREMENTS.md` returns zero matches (string is gone).
    - Diff shows exactly one line modified inside the `### DI Hook (DI)` section.
  </acceptance_criteria>
  <done>DI-03 wording aligned with CONTEXT.md D-01; no other lines in REQUIREMENTS.md are touched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP.md Phase 5 SC #1, SC #4, and Plans block</name>
  <read_first>
    - .planning/ROADMAP.md (full file; focus on the `### Phase 5: ...` block, roughly lines 97–113)
    - .planning/phases/05-adapter-packages-build-docs-migration-publish/05-CONTEXT.md §"spec_lock" (the planner-rewrite text for SC #1) + §"D-01" + §"D-15"
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Three edits inside the `### Phase 5: Adapter Packages, Build, Docs, Migration, Publish` block:

    **Edit A — Goal line (under `**Goal**:`).** Replace the existing one-line goal:

    `**Goal**: Ship a publishable v1.0.0: monorepo build pipeline, dual ESM+CJS distribution verified by \`attw\`/\`publint\`, CI matrix, TypeDI adapter, migration guide, and a runnable README — all on npm under provenance.`

    with:

    `**Goal**: Ship a publishable v1.0.0: single-package build pipeline (one \`package.json\`, one \`src/\`, one \`dist/\`), dual ESM+CJS distribution verified by \`attw\`/\`publint\`, CI matrix, docs-only TypeDI \`useContainer\` recipe, migration guide, and a runnable README — all on npm under provenance.`

    **Edit B — Success Criterion #1.** Replace existing SC #1 text:

    `1. The monorepo (pnpm workspaces, \`packages/core\` + \`packages/typedi\`) builds dual ESM+CJS via \`tshy\` against TypeScript 5.8+; \`prepublishOnly\` runs \`attw\` and \`publint\` green; CI matrix passes across Node 20/22/24, and Vitest 3 suites pass under both \`pool: 'forks'\` and \`pool: 'threads'\`.`

    with:

    `1. The single-package repo (one \`package.json\`, one \`src/\`, one \`dist/\`; no workspaces, no \`packages/*\`) builds dual ESM+CJS via \`tshy\` against TypeScript 5.8+; \`prepublishOnly\` runs \`attw\` and \`publint\` green; CI matrix passes across Node 20/22/24 (Linux), and Vitest 3 suites pass under both \`pool: 'forks'\` and \`pool: 'threads'\`.`

    **Edit C — Success Criterion #4.** Replace existing SC #4 text:

    `4. A separate \`@scope/express-controllers-typedi\` adapter package is published alongside core and demonstrably wires TypeDI into \`useContainer(IocAdapter)\` in an example app.`

    with:

    `4. README documents a \`useContainer\` recipe wiring TypeDI (and any \`.get(token)\`-shaped container — tsyringe, Awilix, etc.) into the core \`useContainer(IocAdapter)\` hook with a runnable example; no separate adapter package is published (single-package rule).`

    **Edit D — Plans block.** Replace the entire `**Plans**: 6 plans` block (currently listing `04-01-PLAN.md` … `04-06-PLAN.md` — a copy-paste from Phase 4) with EXACTLY:

    ```
    **Plans**: 7 plans
      - [ ] 05-01-PLAN.md — Doc rewrites: REQUIREMENTS.md DI-03 + ROADMAP.md SC #1/#4 + Plans block (Wave 1)
      - [ ] 05-02-PLAN.md — pnpm 10 migration + package.json mutations + tshy 3.3.2 bootstrap + biome.json (Wave 1)
      - [ ] 05-03-PLAN.md — Build smoke test (CJS+ESM + emitDecoratorMetadata) + attw/publint gates + single-package grep gate (Wave 2)
      - [ ] 05-04-PLAN.md — GitHub Actions CI workflow: Node 20/22/24 × {forks, threads} matrix + lint + build + attw + publint (Wave 2)
      - [ ] 05-05-PLAN.md — README (30-line Zod example + Validators section + DI recipe) + MIGRATION.md + LICENSE + CONTRIBUTING.md (Wave 2)
      - [ ] 05-06-PLAN.md — Changesets bootstrap (pre enter rc) + TypeDoc + GitHub Actions release workflow with provenance (Wave 3)
      - [ ] 05-07-PLAN.md — RC publish (1.0.0-rc.1) + fresh-project smoke install rehearsal (Wave 4, autonomous: false)
    ```

    Do NOT touch the `**Depends on**`, `**Requirements**`, or `**UI hint**` lines. Do NOT touch SC #2, #3, #5. Do NOT touch any other phase block.
  </action>
  <verify>
    <automated>grep -c "single-package" .planning/ROADMAP.md && grep -F "05-07-PLAN.md — RC publish" .planning/ROADMAP.md && ! grep -F "monorepo (pnpm workspaces, \`packages/core\` + \`packages/typedi\`)" .planning/ROADMAP.md && ! grep -F "A separate \`@scope/express-controllers-typedi\` adapter package is published alongside core" .planning/ROADMAP.md && ! grep -E "^\s+- \[x\] 04-0[1-6]-PLAN.md" .planning/ROADMAP.md | head -1 | grep -q "Wave 3$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "single-package repo (one \`package.json\`, one \`src/\`, one \`dist/\`; no workspaces, no \`packages/*\`)" .planning/ROADMAP.md` returns exactly 1 match (in Phase 5 SC #1).
    - `grep -F "monorepo (pnpm workspaces" .planning/ROADMAP.md` returns 0 matches.
    - `grep -F "A separate \`@scope/express-controllers-typedi\` adapter package is published alongside core" .planning/ROADMAP.md` returns 0 matches.
    - `grep -F "useContainer\` recipe wiring TypeDI" .planning/ROADMAP.md` returns at least 1 match (in Phase 5 SC #4).
    - `grep -cE "^\s+- \[ \] 05-0[1-7]-PLAN.md" .planning/ROADMAP.md` returns exactly 7.
    - `grep -E "^\s+- \[x\] 04-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns ≤ 6 matches (the legitimate Phase 4 entries inside the `### Phase 4` block — none should remain inside the `### Phase 5` block).
    - Phase 4's own block is unchanged: `awk '/^### Phase 4:/,/^### Phase 5:/' .planning/ROADMAP.md | grep -c "04-0"` is unchanged from before the edit (capture pre-edit count and compare).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 5 block reflects single-package direction, docs-only DI recipe, and the seven 05-NN-PLAN.md filenames; Phase 4 block untouched.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -F "useContainer" .planning/REQUIREMENTS.md` matches the new DI-03 line.
2. `grep -F "single-package" .planning/ROADMAP.md` matches the new SC #1 line.
3. `grep -cE "^\s+- \[ \] 05-0[1-7]-PLAN.md" .planning/ROADMAP.md` returns 7.
4. `grep -F "monorepo (pnpm workspaces, \`packages/core\` + \`packages/typedi\`)" .planning/ROADMAP.md` returns 0.
5. `grep -F "A separate \`@scope/express-controllers-typedi\` adapter package" .planning/ROADMAP.md` returns 0.
</verification>

<success_criteria>
REQUIREMENTS.md DI-03 and ROADMAP.md Phase 5 (Goal, SC #1, SC #4, Plans block) reflect the locked single-package + docs-only-DI direction. No other phase blocks touched. No code files touched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adapter-packages-build-docs-migration-publish/05-01-SUMMARY.md`.
</output>
