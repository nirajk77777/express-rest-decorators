---
phase: 01-metadata-decorator-skeleton
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - BUILD-04
  - BUILD-05

must_haves:
  truths:
    - "REQUIREMENTS.md BUILD-04 wording matches CLAUDE.md Direction Override (legacy experimentalDecorators + reflect-metadata)"
    - "REQUIREMENTS.md BUILD-05 wording reflects reflect-metadata IS a core dep"
    - "REQUIREMENTS.md BUILD-06 wording reflects single-package repo (no monorepo)"
    - "STATE.md 'Key Decisions Locked-In' bullets reflect new direction"
    - "Out of Scope class-validator rationale no longer references Stage 3"
    - "Requirement IDs unchanged; traceability table intact"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Up-to-date phase 1 requirement wording"
      contains: "experimentalDecorators: true"
    - path: ".planning/STATE.md"
      provides: "Up-to-date locked decisions block"
      contains: "reflect-metadata"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "CLAUDE.md Direction Override"
      via: "consistent direction wording"
      pattern: "experimentalDecorators: true"
---

<objective>
Rewrite stale REQUIREMENTS.md and STATE.md text so it matches the CLAUDE.md Direction Override (2026-05-08) before any implementation begins.

Purpose: D-01 and D-02 in CONTEXT.md mandate this surgical doc rewrite as a pre-planning chore so downstream agents read consistent direction across CLAUDE.md, ROADMAP.md, REQUIREMENTS.md, and STATE.md. Current text in BUILD-04/05/06 still describes Stage 3 / no-reflect-metadata / monorepo.

Output: Updated REQUIREMENTS.md and STATE.md with corrected wording. No requirement IDs renumbered (D-03). No code changes.

Wave 0 = pre-implementation chore semantics; functionally equivalent to D-02's "before /gsd-plan-phase 1" intent — docs land before any code in Wave 1+.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-metadata-decorator-skeleton/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md BUILD-04, BUILD-05, BUILD-06 and Out-of-Scope class-validator line</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file)
    - .planning/phases/01-metadata-decorator-skeleton/01-CONTEXT.md (D-01, D-02, D-03)
    - CLAUDE.md (Direction Override 2026-05-08 — authoritative)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Surgical edits, NO renumbering of requirement IDs:

    1. Replace BUILD-04 with: "Library uses legacy TypeScript decorators (`experimentalDecorators: true` + `emitDecoratorMetadata: true`); installs a runtime guard that throws an actionable error if either flag is missing or if `reflect-metadata` has not been imported by the consumer."

    2. Replace BUILD-05 with: "Library imports `reflect-metadata` as a runtime dependency in core for reading TS-emitted type metadata (`design:paramtypes`, `design:returntype`, `design:type`); consumers must `import 'reflect-metadata'` once at app entry (documented in README)."

    3. Replace BUILD-06 with: "Repo is a single-package repo (one `package.json`, one `src/`, one `dist/`); dual ESM+CJS published from the package root via `tshy`. Optional integrations live as sub-path exports within the same package."

    4. In the `## Out of Scope` section, replace the line `- **class-validator support** — incompatible with Stage 3 decorators (requires \`experimentalDecorators\` + \`reflect-metadata\`)` with: `- **class-validator support** — out of scope for v1; the technical blocker (Stage 3 incompatibility) no longer applies under the legacy decorator direction, but scope remains v1.x at earliest.`

    5. Also remove the line `- **Parameter decorators (\`@Param\`, \`@Body\`, \`@QueryParam\` as parameter decorators)** — Stage 3 doesn't support them; replaced by method-level input declaration` rationale phrase "Stage 3 doesn't support them" — replace with: `- **Parameter decorators (\`@Param\`, \`@Body\`, \`@QueryParam\` as parameter decorators)** — replaced by method-level input declaration; cleaner type inference and avoids per-arg decorator boilerplate.`

    6. Also rewrite the line `- **\`reflect-metadata\` in core** — quarantined to optional adapter packages if ever needed` to be removed entirely from Out of Scope (it is now IN scope — core dep).

    7. Do NOT modify the traceability table at the bottom (per D-03). Phase assignments stay identical.

    8. Update the `Last updated` field to `2026-05-08`.
  </action>
  <verify>
    <automated>grep -q "experimentalDecorators: true" .planning/REQUIREMENTS.md && grep -q "reflect-metadata" .planning/REQUIREMENTS.md && grep -q "single-package repo" .planning/REQUIREMENTS.md && ! grep -q "Stage 3 decorators only" .planning/REQUIREMENTS.md && ! grep -q "pnpm workspaces monorepo" .planning/REQUIREMENTS.md && ! grep -q "zero \`reflect-metadata\` dependency" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "experimentalDecorators: true" .planning/REQUIREMENTS.md` returns at least one hit
    - `grep "single-package repo" .planning/REQUIREMENTS.md` returns at least one hit
    - `grep "Stage 3 decorators only" .planning/REQUIREMENTS.md` returns zero hits
    - `grep "pnpm workspaces monorepo" .planning/REQUIREMENTS.md` returns zero hits
    - `grep "zero \`reflect-metadata\` dependency" .planning/REQUIREMENTS.md` returns zero hits
    - `grep -c "BUILD-0" .planning/REQUIREMENTS.md` count unchanged from before edit (run `git diff --stat` to confirm IDs intact)
  </acceptance_criteria>
  <done>BUILD-04/05/06 wording matches new direction; class-validator rationale updated; no requirement IDs renumbered; traceability table unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite STATE.md "Key Decisions Locked-In" block</name>
  <read_first>
    - .planning/STATE.md (full file)
    - .planning/phases/01-metadata-decorator-skeleton/01-CONTEXT.md (D-01, D-02)
    - CLAUDE.md (Direction Override)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    Surgical edits to the `### Key Decisions Locked-In (from research)` block:

    1. Replace `- **Decorators:** TC39 Stage 3 only; runtime guard rejects \`experimentalDecorators: true\` consumers.` with: `- **Decorators:** Legacy TypeScript decorators only (\`experimentalDecorators: true\` + \`emitDecoratorMetadata: true\`); runtime guard throws if either flag is missing or \`reflect-metadata\` is not imported.`

    2. Replace `- **Metadata:** Per-class via \`Symbol.metadata\` + WeakMap; no module-level mutable global registry.` with: `- **Metadata:** Hybrid — module-private WeakMaps for library-owned state (keyed by class constructor and prototype); \`reflect-metadata\` ONLY for TS-emitted keys (\`design:paramtypes\`, \`design:returntype\`, \`design:type\`). No module-level mutable global registry. No \`Reflect.defineMetadata\` use by core.`

    3. Replace `- **Repo:** pnpm workspaces monorepo (\`packages/core\` + \`packages/typedi\`).` with: `- **Repo:** Single-package repo (one \`package.json\`, one \`src/\`, one \`dist/\`); optional adapter integrations as sub-path exports.`

    4. Replace `- **\`reflect-metadata\`:** banned from core; quarantined to optional adapter packages only.` with: `- **\`reflect-metadata\`:** core runtime dependency, used exclusively for reading TS-emitted type metadata; consumer must \`import 'reflect-metadata'\` at app entry.`

    5. Replace `- **API shape:** method-level input declaration \`@Get('/:id', { params, query, body })\` with destructured handler args (forced by Stage 3, not chosen).` with: `- **API shape:** method-level input declaration \`@Get('/:id', { params, query, body })\` with destructured handler args (chosen for cleaner type inference).`

    6. Update Project Reference block — replace `TC39 Stage 3 decorators, pluggable validators` with `legacy TypeScript decorators + reflect-metadata, pluggable validators`.

    7. Update `last_updated:` frontmatter to `"2026-05-08T00:00:00.000Z"`.

    8. In the `### Open Questions` section, remove the line `- **Exact Stage 3 decorator generic signatures** — Phase 1 research-flag item; resolve during \`/gsd-plan-phase 1\`.` (resolved by 01-RESEARCH.md), and replace `- **Class-validator legacy adapter** — currently Out of Scope for v1 per REQUIREMENTS.md; revisit at v1.x if migration users demand it.` with `- **Class-validator legacy adapter** — Out of Scope for v1; technical blocker no longer applies under legacy decorators, but scope decision unchanged. Revisit at v1.x.`
  </action>
  <verify>
    <automated>grep -q "Legacy TypeScript decorators only" .planning/STATE.md && grep -q "Single-package repo" .planning/STATE.md && grep -q "core runtime dependency" .planning/STATE.md && ! grep -q "TC39 Stage 3 only" .planning/STATE.md && ! grep -q "pnpm workspaces monorepo" .planning/STATE.md && ! grep -q "banned from core" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Legacy TypeScript decorators only" .planning/STATE.md` returns one hit
    - `grep "Single-package repo" .planning/STATE.md` returns at least one hit
    - `grep "TC39 Stage 3 only" .planning/STATE.md` returns zero hits
    - `grep "pnpm workspaces monorepo" .planning/STATE.md` returns zero hits
    - `grep "banned from core" .planning/STATE.md` returns zero hits
    - frontmatter `last_updated` field equals `"2026-05-08T00:00:00.000Z"`
  </acceptance_criteria>
  <done>STATE.md "Key Decisions Locked-In" block fully aligned with CLAUDE.md Direction Override; Open Questions cleaned of resolved items.</done>
</task>

</tasks>

<verification>
- Run all acceptance grep checks above
- `git diff .planning/REQUIREMENTS.md .planning/STATE.md` shows ONLY wording edits in the targeted lines, no requirement-ID renumbering, no traceability-table modification
- `grep -c "^- \[ \] \*\*BUILD-" .planning/REQUIREMENTS.md` count is unchanged
</verification>

<success_criteria>
REQUIREMENTS.md and STATE.md no longer contain Stage 3 / monorepo / no-reflect-metadata wording in the targeted sections; all requirement IDs preserved; traceability table untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-metadata-decorator-skeleton/01-01-SUMMARY.md`.
</output>
