---
phase: 05-adapter-packages-build-docs-migration-publish
plan: 07
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - CHANGELOG.md
  - package.json
autonomous: false
requirements: [DOCS-04]

user_setup:
  - service: npm
    why: "Publishing the package requires write access to the @nirajk scope on npmjs.com"
    env_vars:
      - name: NPM_TOKEN
        source: "https://www.npmjs.com/settings/<user>/tokens — create a 'Granular Access Token' with publish permission scoped to @nirajk/*"
    dashboard_config:
      - task: "Add NPM_TOKEN as a GitHub Actions secret"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
      - task: "Enable GitHub Pages on the repo (Source: gh-pages branch, root path)"
        location: "GitHub repo → Settings → Pages"
      - task: "(Optional) Verify the @nirajk npm scope exists and you own it"
        location: "https://www.npmjs.com/settings/<user>/packages"

must_haves:
  truths:
    - "`pnpm changeset version` produces version 1.0.0-rc.1, updates CHANGELOG.md, and bumps package.json#version (D-03)"
    - "Local pre-flight pipeline passes: pnpm install --frozen-lockfile + lint + test:forks + test:threads + build + test:build + attw + publint all green"
    - "After user approval (the lone manual gate in this plan), `pnpm release` publishes 1.0.0-rc.1 to npm under dist-tag 'next' with provenance (or via the Changesets-merge flow on GitHub Actions)"
    - "`npm view @nirajk/express-controllers@1.0.0-rc.1` returns the package metadata from the registry"
    - "`npm view @nirajk/express-controllers dist-tags` shows 'next' pointing to 1.0.0-rc.1 (NOT 'latest')"
    - "Smoke install: a fresh /tmp project can `pnpm add @nirajk/express-controllers@next zod express reflect-metadata` and run the README's 30-line example end-to-end (DOCS-01 acceptance test)"
    - "(Optional, if release.yml fired) GitHub Pages docs site is live at the URL written into README"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog 1.0.0-rc.1 entry generated by `changeset version` (DOCS-04)"
      contains: "1.0.0-rc.1"
    - path: "package.json"
      provides: "version field bumped to 1.0.0-rc.1"
      contains: "1.0.0-rc.1"
  key_links:
    - from: ".changeset/initial-release.md (Plan 05-06)"
      to: "CHANGELOG.md '## 1.0.0-rc.1' entry"
      via: "pnpm changeset version"
      pattern: "1.0.0-rc.1"
    - from: "package.json#version 1.0.0-rc.1"
      to: "npm registry @nirajk/express-controllers@1.0.0-rc.1"
      via: "pnpm release (changeset publish --provenance)"
      pattern: "1.0.0-rc.1"
---

<objective>
Ship the first artifact. Three deliverables:

1. **Version bump + CHANGELOG generation** — `pnpm changeset version` consumes the seed changeset from Plan 05-06, writes `CHANGELOG.md` (Keep-a-Changelog format, DOCS-04), bumps `package.json#version` to `1.0.0-rc.1` (because pre-mode tag is `rc`).

2. **Manual approval checkpoint** — autonomous: false. After the local pre-flight gates pass, pause and let the user choose between two publish paths:
   - **Path A (recommended): GitHub Actions release flow.** User commits + pushes to main; Changesets bot opens the "Version Packages" PR; merging it triggers `release.yml` which runs `pnpm release` with provenance. This is the canonical D-08 flow and exercises the entire release pipeline.
   - **Path B: Local publish.** Run `pnpm release` locally with `NPM_TOKEN` exported. Faster but skips the GitHub Actions provenance OIDC story (uses the local npm token instead). Good fallback if the GitHub Actions plumbing has a bug we want to find later.

3. **Post-publish smoke install (RC rehearsal, D-03)** — in a fresh `/tmp/<rand>` directory, `pnpm init -y && pnpm add @nirajk/express-controllers@next zod express reflect-metadata`, paste the README's 30-line example into `index.ts`, run it with `tsx index.ts`, hit `GET /users/42` and `POST /users` with curl, observe valid JSON responses. This is the DOCS-01 acceptance test.

This plan is `autonomous: false` because publishing to npm is a one-way operation — once a version is published, even an `npm unpublish` within 24 hours leaves a record. The user MUST approve the publish path explicitly.

Purpose: ship 1.0.0-rc.1 to npm as a smoke test of the entire pipeline (build + attw + publint + provenance + GitHub Pages + smoke install). After this RC rehearses cleanly, a follow-up plan can `pnpm changeset pre exit && pnpm changeset version && push` to ship 1.0.0 to dist-tag `latest`.
Output: CHANGELOG.md committed; `@nirajk/express-controllers@1.0.0-rc.1` published to npm; smoke-install passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@.changeset/initial-release.md
@.changeset/config.json
@.changeset/pre.json
@.github/workflows/release.yml
@.github/workflows/ci.yml
@README.md
@.planning/phases/05-adapter-packages-build-docs-migration-publish/05-CONTEXT.md
@.planning/phases/05-adapter-packages-build-docs-migration-publish/05-RESEARCH.md
@.planning/phases/05-adapter-packages-build-docs-migration-publish/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local pre-flight + version bump + CHANGELOG generation</name>
  <read_first>
    - .changeset/initial-release.md (the seed changeset that will become the CHANGELOG entry)
    - .changeset/pre.json (confirms tag: 'rc' — controls the version suffix)
    - package.json (current version 0.0.0; must bump to 1.0.0-rc.1)
    - .planning/phases/05-adapter-packages-build-docs-migration-publish/05-RESEARCH.md §"Pattern 6" — exact `changeset version` behavior under pre mode
    - .planning/phases/05-adapter-packages-build-docs-migration-publish/05-06-SUMMARY.md
  </read_first>
  <files>CHANGELOG.md, package.json</files>
  <action>
    **Step 1 — Run the full local pre-flight gate.** Every step must exit 0 before proceeding:

        pnpm install --frozen-lockfile
        bash scripts/check-single-package.sh
        pnpm typecheck
        pnpm lint
        pnpm test:forks
        pnpm test:threads
        pnpm build
        pnpm test:build
        pnpm exec attw --pack . --profile node16
        pnpm exec publint

    If any step fails, STOP and surface the failure. The publish in Task 2 must not proceed against a broken local build.

    **Step 2 — Bump version + generate CHANGELOG.**

        pnpm exec changeset version

    Expected effects:
    - `package.json#version` changes from `0.0.0` to `1.0.0-rc.1` (pre mode `rc` tag combined with the `minor` bump in `.changeset/initial-release.md`; the first version in pre mode for a new package is `0.0.1` plus pre-tag → BUT Changesets recognizes `0.0.0` as un-published-marker and bumps to the appropriate release version under pre-mode → `1.0.0-rc.1`).
    - `CHANGELOG.md` is created at repo root with a `## 1.0.0-rc.1` heading and the Keep-a-Changelog-formatted bullet list from `.changeset/initial-release.md`.
    - `.changeset/initial-release.md` is consumed (deleted by `changeset version`).
    - `.changeset/pre.json` updated to record version transitions.

    Verification on the resulting state:

        jq -r .version package.json
        # Expected: 1.0.0-rc.1
        head -20 CHANGELOG.md
        # Expected: starts with "# @nirajk/express-controllers" or similar header,
        #           then "## 1.0.0-rc.1", then the bullet list.

    If the resulting version is NOT `1.0.0-rc.1` (e.g., it produced `0.1.0-rc.0` because the seed changeset's `minor` bump applied to `0.0.0` rather than to a virtual `1.0.0` baseline):
    - Edit package.json directly: `jq '.version = "1.0.0-rc.1"' package.json > package.json.tmp && mv package.json.tmp package.json`.
    - Manually rewrite the CHANGELOG.md heading from `## 0.1.0-rc.0` to `## 1.0.0-rc.1`.
    - Document the manual override in the SUMMARY.

    Rationale for forcing `1.0.0-rc.1`: D-03 explicitly specifies the RC ladder starts at `1.0.0-rc.1`, not at a `0.x` series.

    **Step 3 — Re-run the full pre-flight against the version-bumped state.**

        pnpm install --frozen-lockfile
        pnpm test:forks
        pnpm build
        pnpm test:build
        pnpm exec attw --pack . --profile node16
        pnpm exec publint

    All must exit 0. The version field appears in the published artifacts; this re-run catches any version-string-related lint or attw issue.

    **Step 4 — Commit but do NOT push yet.** The push happens after Task 2's user decision.

        git status --short
        # Expect: modifications to package.json, .changeset/*, new CHANGELOG.md
        # No code changes.

    Stage only the version + CHANGELOG + .changeset bookkeeping changes:

        git add package.json CHANGELOG.md .changeset/

    Do NOT commit yet. The commit message is finalized in Task 2 based on the chosen publish path.
  </action>
  <verify>
    <automated>jq -r .version package.json | grep -qx '1.0.0-rc.1' &amp;&amp; test -f CHANGELOG.md &amp;&amp; grep -q '1.0.0-rc.1' CHANGELOG.md &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test:forks &amp;&amp; pnpm test:threads &amp;&amp; pnpm build &amp;&amp; pnpm test:build &amp;&amp; pnpm exec attw --pack . --profile node16 &amp;&amp; pnpm exec publint &amp;&amp; bash scripts/check-single-package.sh</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r .version package.json` prints `1.0.0-rc.1`.
    - `CHANGELOG.md` exists at repo root and contains the string `1.0.0-rc.1` at heading depth `## ` or higher.
    - `CHANGELOG.md` contains at least 5 of the bullets from `.changeset/initial-release.md` (e.g., `JsonController`, `useContainer`, `getRequestContext`, `Standard Schema`, `provenance`).
    - All 11 pre-flight commands exit 0 (listed in Step 1 + Step 3).
    - `git diff --cached --name-only` shows only `package.json`, `CHANGELOG.md`, and `.changeset/*` changes (no source-code changes).
    - `.changeset/initial-release.md` no longer exists (consumed by `changeset version`).
  </acceptance_criteria>
  <done>Version bumped to 1.0.0-rc.1; CHANGELOG.md generated; full local pre-flight green; staged for commit.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User approves publish path (GitHub Actions vs local)</name>
  <decision>How should `@nirajk/express-controllers@1.0.0-rc.1` be published — via the GitHub Actions release workflow (D-08 canonical flow) or a one-shot local `pnpm release`?</decision>
  <context>
    Plan 05-06 wired `release.yml` to do the canonical Changesets + provenance + gh-pages flow. The cleanest first-publish exercises that pipeline end-to-end. But the local fallback exists for two reasons:

    1. **NPM_TOKEN secret hasn't been added to GitHub yet.** Path A requires the user to have already pasted `NPM_TOKEN` into GitHub repo Settings → Secrets and variables → Actions. If it isn't there, `release.yml` will fail with "ENEEDAUTH". The user must confirm they've done this before choosing Path A.

    2. **GitHub Pages hasn't been enabled yet.** `release.yml`'s docs-deploy step pushes to `gh-pages` branch — that requires GitHub Pages to be configured to serve from `gh-pages`. If it isn't, the docs deploy will silently no-op (peaceiris/actions-gh-pages creates the branch but Pages won't serve until enabled in Settings).

    A publish is a one-way operation. `npm unpublish` within 24h leaves a tombstone; after 72h it's blocked entirely. The user gets the explicit "yes, publish 1.0.0-rc.1" gate here.

    **What the user should verify BEFORE choosing Path A:**

    - [ ] `NPM_TOKEN` is set in GitHub repo Settings → Secrets → Actions
    - [ ] The token has publish scope for the `@nirajk/*` scope (or is account-wide automation token)
    - [ ] The `@nirajk` npm scope exists and the user owns it
    - [ ] GitHub Pages is enabled (Source: `gh-pages` branch, root) — not strictly required for the npm publish but required for the docs site to come up

    **What the user should verify BEFORE choosing Path B:**

    - [ ] `NPM_TOKEN` is exported in the local shell (`echo $NPM_TOKEN | head -c 10` shows a non-empty prefix)
    - [ ] Logged in as the right npm user (`npm whoami` returns the user owning `@nirajk`)
  </context>
  <options>
    <option id="github-actions">
      <name>Path A — Publish via GitHub Actions (D-08 canonical flow)</name>
      <pros>
        - Exercises the full release pipeline (provenance via OIDC + gh-pages docs deploy)
        - Manual approval gate is the Version Packages PR merge — survives every contributor scenario
        - Provenance attestation on npm UI is real
      </pros>
      <cons>
        - Two-step (push → PR opens → merge) — slower than local
        - Fails if NPM_TOKEN secret isn't configured
      </cons>
    </option>
    <option id="local">
      <name>Path B — Publish locally with `pnpm release`</name>
      <pros>
        - One-shot, fast feedback
        - No dependency on GitHub Actions plumbing
      </pros>
      <cons>
        - Misses the OIDC-based provenance attestation (npm UI shows "unverified")
        - Skips the gh-pages docs deploy (must run manually)
        - Doesn't validate release.yml; release.yml's first real exercise becomes the v1.0.0 promotion later
      </cons>
    </option>
    <option id="abort">
      <name>Path C — Abort (don't publish yet)</name>
      <pros>
        - User wants to verify GitHub Pages / NPM_TOKEN setup before committing
        - Gives time to do a final review of the pipeline
      </pros>
      <cons>
        - Phase 5 ends without an actual artifact on npm — the smoke-install step in Task 4 cannot run
      </cons>
    </option>
  </options>
  <resume-signal>
    Reply with one of:
    - `path-a` (or `github-actions` / `pr`) — proceed via Version Packages PR merge on GitHub Actions
    - `path-b` (or `local`) — proceed with local `pnpm release`
    - `path-c` (or `abort` / `wait`) — pause; resume by re-running this task after setup is confirmed
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the chosen publish path</name>
  <read_first>
    - The resume-signal value from Task 2 (path-a / path-b / path-c)
    - .github/workflows/release.yml (Path A reference)
    - package.json#scripts.release (Path B command)
  </read_first>
  <files>(no file edits — orchestrates external publish)</files>
  <action>
    Branch on the Task 2 decision:

    ---

    **If `path-a` (GitHub Actions):**

    1. Commit the staged changes:

           git commit -m "chore(release): version packages (1.0.0-rc.1)"

    2. Push to main:

           git push origin main

    3. Wait ≤ 5 minutes for the Changesets bot to open / update a "Version Packages" PR. Watch the GitHub Actions tab for the `Release` workflow run.

    4. The bot's PR may already be there — but in pre-mode with the changeset already consumed in Task 1, Changesets behavior is:
       - If `.changeset/pre.json` records the consumed version, the bot won't reopen a Version Packages PR for it; the push to main with the bumped version + CHANGELOG triggers `changesets/action@v1` to detect there's nothing to "version" but a publish is needed → it runs `publish: pnpm release` directly.
       - This is the publish moment. Watch the run; the `Create release PR or publish` step will take ~3–5 minutes (build + attw + publint + npm publish + provenance + gh-pages deploy).

    5. After the run is green:

           npm view @nirajk/express-controllers dist-tags
           # Expected: dist-tag 'next' points to '1.0.0-rc.1' (per D-03; the locked decision overrides Changesets' default 'rc' tag).
           # If Changesets published under 'rc' instead of 'next', re-tag immediately:
           #   npm dist-tag add @nirajk/express-controllers@1.0.0-rc.1 next
           #   npm dist-tag rm @nirajk/express-controllers rc   # only if 'rc' was added

           npm view @nirajk/express-controllers@1.0.0-rc.1
           # Expected: package metadata, including provenance.

       **Resolving the D-03 vs Changesets pre-mode discrepancy (per checker revision 2026-05-10):** Changesets pre-mode auto-publishes under the pre-tag name (`rc`), but D-03 locks the dist-tag as `next`. To honor D-03:
       - The release.yml workflow's `pnpm release` invocation (Plan 05-06) MUST pass `--tag next` to `changeset publish`. If `release.yml` was authored with bare `pnpm release` and `pnpm release` expands to `changeset publish --provenance` without `--tag next`, the canonical fix is to update `package.json#scripts.release` to: `pnpm build && attw --pack . --profile node16 && publint && changeset publish --provenance --tag next` OR override directly in release.yml with: `pnpm build && pnpm exec attw --pack . --profile node16 && pnpm exec publint && npm publish --tag next --provenance`.
       - Verify after the workflow run: `npm view @nirajk/express-controllers dist-tags` shows `next: 1.0.0-rc.1`. If it shows `rc: 1.0.0-rc.1`, run the dist-tag re-tagging shown above and document in the SUMMARY for Plan 05-06 to fix release.yml going forward.

    ---

    **If `path-b` (local):**

    1. Confirm token + identity:

           npm whoami    # must return the @nirajk owner
           # NPM_TOKEN must already be in your shell or in ~/.npmrc

    2. Commit:

           git commit -m "chore(release): version packages (1.0.0-rc.1)"

    3. Run the publish (per D-03, dist-tag MUST be 'next'; explicitly override Changesets' default 'rc' pre-mode tag):

           # Primary path: try `pnpm release` with explicit --tag next forwarded to changeset publish.
           # If package.json#scripts.release does NOT include --tag next, prefer the direct path below.
           pnpm release    # only if scripts.release includes --tag next

           # Direct path (RECOMMENDED for first publish — bypasses Changesets dist-tag auto-assignment):
           pnpm build && pnpm exec attw --pack . --profile node16 && pnpm exec publint
           npm publish --tag next --provenance --access public

       Expected output: `npm notice` lines including a `Provenance` block, followed by `+ @nirajk/express-controllers@1.0.0-rc.1`. The dist-tag MUST be `next` (per D-03 locked decision), NOT `rc`.

       After the publish, verify:

           npm view @nirajk/express-controllers dist-tags
           # Expected: { next: '1.0.0-rc.1' }

       If `dist-tags` shows `rc: 1.0.0-rc.1` instead, re-tag immediately:

           npm dist-tag add @nirajk/express-controllers@1.0.0-rc.1 next
           # leave 'rc' present too if Changesets added it; not harmful, but document in SUMMARY

       Document which path actually ran in the SUMMARY (direct npm publish vs `pnpm release` with `--tag next` honored).

    4. Push the commit (with tag):

           pnpm exec changeset tag      # creates the v1.0.0-rc.1 git tag
           git push --follow-tags origin main

    5. Verify:

           npm view @nirajk/express-controllers@1.0.0-rc.1 dist-tags

    ---

    **If `path-c` (abort):**

    1. Reset the staged changes:

           git restore --staged package.json CHANGELOG.md .changeset/

    2. Optionally `git stash` the working-tree changes to preserve them.

    3. Stop the plan. Resume by re-running Task 1 (re-run pre-flight) → Task 2 (decide again) once setup is verified.

       NOTE: the `pnpm changeset version` work from Task 1 is reproducible — re-running it on a clean tree replays the bump; no permanent state was changed by Task 1 except the working tree.
  </action>
  <verify>
    <automated>case "${RESUME_SIGNAL:-path-a}" in path-c|abort) test -z "$(git diff --cached --name-only)";; *) npm view @nirajk/express-controllers@1.0.0-rc.1 version 2>/dev/null | grep -qx '1.0.0-rc.1';; esac</automated>
  </verify>
  <acceptance_criteria>
    - For path-a or path-b: `npm view @nirajk/express-controllers@1.0.0-rc.1 version` prints `1.0.0-rc.1`.
    - For path-a or path-b: `npm view @nirajk/express-controllers dist-tags` shows `next: 1.0.0-rc.1` (per D-03 locked decision); does NOT show it under `latest`. If Changesets pre-mode published under `rc` instead, the executor must re-tag to `next` before declaring this task done.
    - For path-a or path-b: the published tarball appears in the npm registry's web UI; `npm view @nirajk/express-controllers@1.0.0-rc.1` returns metadata; if the run was via GitHub Actions OIDC, the npm UI shows a Provenance badge.
    - For path-a: `https://github.com/<user>/<repo>/actions` shows a green `Release` workflow run.
    - For path-c: the working tree retains the version bump but no commit/push/publish occurred; user can resume the plan later.
  </acceptance_criteria>
  <done>Either `@nirajk/express-controllers@1.0.0-rc.1` is published to npm OR the plan was explicitly aborted by the user.</done>
</task>

<task type="auto">
  <name>Task 4: Fresh-project smoke install (DOCS-01 acceptance test, D-03 RC rehearsal)</name>
  <read_first>
    - README.md (extract the 30-line example exactly as it appears)
    - .planning/phases/05-adapter-packages-build-docs-migration-publish/05-CONTEXT.md §"D-03" + §"D-10"
  </read_first>
  <files>(creates and tears down a /tmp project — no repo files modified)</files>
  <action>
    Skip this task if Task 3 was `path-c` (abort). Otherwise:

    1. Create a fresh smoke-install workspace:

           SMOKE=$(mktemp -d -t express-controllers-smoke-XXXXXX)
           cd "$SMOKE"
           pnpm init

    2. Install the published RC + dependencies:

           pnpm add @nirajk/express-controllers@next zod express reflect-metadata
           pnpm add -D typescript tsx @types/express @types/node

    3. Create `tsconfig.json`:

           {
             "compilerOptions": {
               "target": "ES2022",
               "module": "NodeNext",
               "moduleResolution": "NodeNext",
               "experimentalDecorators": true,
               "emitDecoratorMetadata": true,
               "useDefineForClassFields": false,
               "strict": true,
               "esModuleInterop": true
             }
           }

    4. Create `index.ts` containing the EXACT 30-line example from the README:

       Extract via:

           awk '/^```typescript$/{flag=1;next} /^```$/{flag=0;exit} flag' /Users/niraj/Desktop/Projects/routing-controlles-express/README.md > index.ts

       Verify `index.ts` contains `JsonController`, `Get`, `Post`, `useExpressControllers`, and `import 'reflect-metadata'`.

    5. Run it:

           pnpm exec tsx index.ts &
           SERVER_PID=$!
           sleep 2

    6. Smoke the routes via curl (the README example exposes `GET /users/:id` and `POST /users`):

           curl -sf http://localhost:3000/users/42
           # Expected: JSON like {"id":42,"name":"Alice"}

           curl -sf -X POST http://localhost:3000/users \
             -H 'Content-Type: application/json' \
             -d '{"name":"Bob","email":"bob@example.com"}'
           # Expected: JSON like {"created":true,"user":{"name":"Bob","email":"bob@example.com"}}

    7. Negative smoke — validation should reject malformed input:

           curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/users \
             -H 'Content-Type: application/json' \
             -d '{"name":123}'
           # Expected: 400

    8. Cleanup:

           kill $SERVER_PID
           cd -
           rm -rf "$SMOKE"

    Record outcomes in the SUMMARY:
    - All three curls succeeded?
    - Did the server start without a `reflect-metadata` runtime guard error?
    - Did the validation reject (400) work?

    Any failure here is a release-blocker — surface immediately, do NOT silently swallow. The fix is likely an additional RC bump (`pnpm changeset add` for the fix; `pnpm changeset version` → `1.0.0-rc.2`; re-publish).
  </action>
  <verify>
    <automated>true # Manual verification via curl outputs in stdout — see action steps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm add @nirajk/express-controllers@next` succeeds in the smoke project.
    - `pnpm exec tsx index.ts` starts the server without throwing the legacy-decorator runtime guard.
    - `curl http://localhost:3000/users/42` returns 2xx JSON.
    - `curl -X POST http://localhost:3000/users` with valid JSON body returns 2xx with the echoed user.
    - `curl -X POST http://localhost:3000/users` with `{"name":123}` returns HTTP 400 (Zod validation rejects through BadRequestError).
    - The server gracefully exits when killed.
    - Total smoke-install run completes in &lt; 60 seconds (excluding `pnpm add` install time).
  </acceptance_criteria>
  <done>Fresh-project smoke install proved the published RC works end-to-end: install, decorator metadata, routing, validation, JSON serialization. DOCS-01 acceptance criterion satisfied against a real consumer scenario.</done>
</task>

</tasks>

<verification>
After all four tasks (when not aborted):
1. `npm view @nirajk/express-controllers@1.0.0-rc.1 version` prints `1.0.0-rc.1`.
2. `npm view @nirajk/express-controllers dist-tags` shows the version under `rc` or `next` (NOT `latest`).
3. `git log -1 --format=%s` includes a `chore(release): version packages` commit.
4. CHANGELOG.md, package.json (version: 1.0.0-rc.1), and the consumed `.changeset/` state are committed.
5. The fresh-project smoke install ran the 30-line README example end-to-end with passing curl smoke + Zod validation rejection.
6. (Path A only) `release.yml` workflow run is green and `https://<user>.github.io/<repo>/` serves the TypeDoc HTML.
</verification>

<success_criteria>
DOCS-04 (Keep-a-Changelog + Changesets + provenance) is verified end-to-end. The library exists on npm. The README's 30-line example demonstrably works in a fresh consumer project — DOCS-01's acceptance test passes against a real published artifact, not a local build. Phase 5 succeeds.

Follow-up (NOT in this plan; trigger as a separate plan or quick PR after RC stabilizes):
- `pnpm exec changeset pre exit && pnpm exec changeset version` to drop the `-rc.N` suffix → `1.0.0` under dist-tag `latest`
- Update README dist-tag badges from `next` → `latest`
- Tag and announce v1.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/05-adapter-packages-build-docs-migration-publish/05-07-SUMMARY.md` recording: chosen publish path (a/b/c), npm dist-tag the RC ended up under (`rc` vs `next` — Changesets pre-mode behavior detail), whether `--provenance` actually applied (RESEARCH Assumption A1 verified or fallback used), GitHub Pages URL live state, smoke-install full curl outputs, and any rc.2 changeset queued in response to smoke findings.
</output>
