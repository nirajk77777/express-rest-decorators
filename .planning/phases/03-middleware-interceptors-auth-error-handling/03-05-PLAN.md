---
phase: 03-middleware-interceptors-auth-error-handling
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - tests/integration/phase3/ordering-fixture.test.ts
  - tests/integration/phase3/middleware-class-form.test.ts
  - tests/integration/phase3/interceptor-pipeline.test.ts
  - tests/integration/phase3/auth-pipeline.test.ts
  - tests/integration/phase3/user-error-mw.test.ts
  - tests/integration/phase3/grep-gate.test.ts
autonomous: true
requirements: [MW-01, MW-02, MW-03, MW-04, AUTH-01, AUTH-02, AUTH-03, ERR-04]
must_haves:
  truths:
    - "ROADMAP Phase 3 SC#1 verified: function and class form @UseBefore/@UseAfter at controller and method level execute in deterministic top-to-bottom order (controller-before, method-before, then method-after, controller-after) — proven by ordering fixture."
    - "ROADMAP Phase 3 SC#2 verified: global @Middleware({type:'before'/'after'}) classes registered via BootOptions.middlewares execute in the documented outermost order."
    - "ROADMAP Phase 3 SC#3 verified: @Interceptor + @UseInterceptor transforms handler return value before serialization (e.g., wraps {data: ...} envelope)."
    - "ROADMAP Phase 3 SC#4 verified: @Authorized + authorizationChecker + currentUserChecker → 401 vs 403 with currentUser injected via input declaration."
    - "ROADMAP Phase 3 SC#5 verified: user @Middleware({type:'after'}) error class with 4-arg use runs ahead of libraryErrorMiddleware and can format/replace the response."
    - "Grep gate: no Express imports in src/decorators/ or src/metadata/; no Reflect.defineMetadata anywhere in src/decorators/middleware.ts; no try/catch in middleware.ts/interceptor.ts."
  artifacts:
    - path: tests/integration/phase3/ordering-fixture.test.ts
      provides: "MW-04 fixture proving exact execution order with named hooks"
    - path: tests/integration/phase3/middleware-class-form.test.ts
      provides: "Class-form ExpressMiddlewareInterface mw via container DI"
    - path: tests/integration/phase3/interceptor-pipeline.test.ts
      provides: "Global + controller + method interceptor chain proves D-09 order"
    - path: tests/integration/phase3/auth-pipeline.test.ts
      provides: "401/403 paths + currentUser injection + user-thrown HttpError escape hatch"
    - path: tests/integration/phase3/user-error-mw.test.ts
      provides: "ERR-04 user error mw chains and lib default fallback"
    - path: tests/integration/phase3/grep-gate.test.ts
      provides: "Structural invariants enforced as tests"
  key_links: []
---

<objective>
Prove every ROADMAP Phase 3 success criterion with end-to-end integration tests using supertest against a real Express v5 app booted via `useExpressControllers`. Add structural grep-gate tests to lock in the architectural invariants from CONTEXT.md (no Express in metadata layer, no Reflect.defineMetadata in decorators, no try/catch wrapping in middleware/interceptor).

Purpose: Plans 01-04 are unit-tested in isolation. This plan proves they compose correctly under a real HTTP request — the only verification ROADMAP success criteria accept.

Output:
- Six integration test files under `tests/integration/phase3/` covering SC#1–SC#5 plus structural gates.
- All tests pass; total Phase 3 test count documented in summary.
- NO source code changes — purely test additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-middleware-interceptors-auth-error-handling/03-CONTEXT.md
@.planning/phases/03-middleware-interceptors-auth-error-handling/03-RESEARCH.md
@.planning/phases/03-middleware-interceptors-auth-error-handling/03-04-SUMMARY.md
@CLAUDE.md
@src/index.ts
@src/adapter/boot.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#1 + SC#2 ordering fixture (MW-01, MW-02, MW-04)</name>
  <read_first>
    - .planning/phases/03-middleware-interceptors-auth-error-handling/03-CONTEXT.md §D-01 + §D-02 (canonical pipeline order + level-reversal rule)
    - tests/integration/ (existing Phase 2 supertest pattern — copy boot setup)
    - src/index.ts (verify all decorators + interfaces are exported and importable)
  </read_first>
  <files>tests/integration/phase3/ordering-fixture.test.ts, tests/integration/phase3/middleware-class-form.test.ts</files>
  <behavior>
    ordering-fixture.test.ts: A controller decorated as:
    ```ts
    @Middleware({ type: 'before' })
    class GlobalBeforeMw { use(req, res, next) { trace.push('global-before'); next(); } }

    @Middleware({ type: 'after' })
    class GlobalAfterMw { use(req, res, next) { trace.push('global-after'); next(); } }

    const ctrlBeforeFn = (req, res, next) => { trace.push('ctrl-before-fn1'); next(); };
    const ctrlBeforeFn2 = (req, res, next) => { trace.push('ctrl-before-fn2'); next(); };
    const methBeforeFn = (req, res, next) => { trace.push('method-before'); next(); };
    const methAfterFn = (req, res, next) => { trace.push('method-after'); next(); };
    const ctrlAfterFn = (req, res, next) => { trace.push('ctrl-after'); next(); };

    @JsonController('/test')
    @UseBefore(ctrlBeforeFn, ctrlBeforeFn2)
    @UseAfter(ctrlAfterFn)
    class T {
      @Get('/order')
      @UseBefore(methBeforeFn)
      @UseAfter(methAfterFn)
      get() { trace.push('handler'); return { ok: 1 }; }
    }
    ```
    Boot with `middlewares: [GlobalBeforeMw, GlobalAfterMw]`. GET /test/order. Assert trace EQUALS:
    ```
    ['global-before', 'ctrl-before-fn1', 'ctrl-before-fn2', 'method-before', 'handler', 'method-after', 'ctrl-after', 'global-after']
    ```

    Variant 2: prove level-reversal for `@UseAfter` (D-02): controller-level `@UseAfter` runs AFTER method-level `@UseAfter`. The trace already proves this (`method-after` precedes `ctrl-after`).

    Variant 3: prove within-decorator left-to-right for both before AND after: `@UseAfter(a, b, c)` → `a → b → c` (NOT reversed within args).

    middleware-class-form.test.ts:
    - `class LogMw implements ExpressMiddlewareInterface { use(req, res, next) { (req as any).logged = true; next(); } }`
    - `@UseBefore(LogMw)` on a method — verify `req.logged === true` reaches the handler.
    - With container resolution: register a class with constructor dep, set up a custom container via `useContainer({ get: cls => new cls(injectedDep) })`, verify the constructor was called with the injected dep (capture in a Set spy).
  </behavior>
  <action>
1. Inspect `tests/integration/` for the existing Phase 2 supertest setup pattern (e.g. `tests/integration/phase2/<file>.test.ts` if present). Mirror the import shape and `beforeEach(resetContainer)` hooks.
2. Write the two test files exactly per `<behavior>`.
3. Use `import { useExpressControllers, JsonController, Get, UseBefore, UseAfter, Middleware, ExpressMiddlewareInterface } from '../../../src/index.js';`.
4. Use a `let trace: string[] = []` reset in beforeEach.
5. Run.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/phase3/ordering-fixture.test.ts tests/integration/phase3/middleware-class-form.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - `grep -nE "trace\.push\('global-before'\)" tests/integration/phase3/ordering-fixture.test.ts` returns at least 1 match (proves the test asserts the exact string).
    - The trace assertion in ordering-fixture.test.ts uses `toEqual` (or `toStrictEqual`) on the full ordered array — not a permissive `toContain` (verified by `grep -nE 'toEqual|toStrictEqual' tests/integration/phase3/ordering-fixture.test.ts` returning at least 1).
  </acceptance_criteria>
  <done>SC#1 and SC#2 proven; MW-04 fixture in place.</done>
</task>

<task type="auto">
  <name>Task 2: SC#3 interceptor pipeline + SC#4 auth pipeline (MW-03, AUTH-01..03)</name>
  <read_first>
    - .planning/phases/03-middleware-interceptors-auth-error-handling/03-CONTEXT.md §D-07/D-08/D-09 (interceptor) + §D-11/D-12/D-13/D-14 (auth)
    - .planning/phases/03-middleware-interceptors-auth-error-handling/03-04-SUMMARY.md (current handler factory shape from Plan 04)
    - src/interfaces/interceptor.ts (InterceptorInterface)
    - src/index.ts (Interceptor, UseInterceptor, Authorized, currentUserChecker key on BootOptions)
  </read_first>
  <files>tests/integration/phase3/interceptor-pipeline.test.ts, tests/integration/phase3/auth-pipeline.test.ts</files>
  <behavior>
    interceptor-pipeline.test.ts:
    - Global interceptor `@Interceptor() class GlobalI { intercept(a, c) { return { wrapped: c }; } }`.
    - Controller-level `@UseInterceptor(CtrlI)` where `CtrlI.intercept` adds `{ ctrl: true, ...c }`.
    - Method-level `@UseInterceptor(MethI)` where `MethI.intercept` adds `{ meth: true, ...c }`.
    - Handler returns `{ raw: 'value' }`.
    - Assert response body === `{ wrapped: { ctrl: true, meth: true, raw: 'value' } }` per D-09 order: global → ctrl → method.
    - Negative: handler returning `null` → response is 204 with empty body, interceptors NOT invoked (verified via spy `expect(globalSpy).not.toHaveBeenCalled()`).
    - Negative: handler throws → error middleware fires; interceptors NOT invoked (D-10).

    auth-pipeline.test.ts:
    - Case A (no checker): `@Authorized()` route, BootOptions.authorizationChecker = undefined → 401.
    - Case B (checker says false): `@Authorized('admin')` + authorizationChecker that returns false → 403.
    - Case C (checker says true): authChecker returns true → 200 with handler payload.
    - Case D (currentUser falsy): currentUserChecker returns null → 401, authChecker NOT called.
    - Case E (currentUser === false): currentUserChecker returns `false` (the strict-false exception per D-12) → flow continues; authChecker IS called.
    - Case F (currentUser injection): handler `@Get('/me', { currentUser: true })` accepts `({ currentUser })` and echoes back. With currentUserChecker returning `{ id: 42 }` → response body contains `{ id: 42 }`.
    - Case G (escape hatch): authChecker throws `new ForbiddenError('custom message')` → response status 403 with body `{ status: 403, name: 'ForbiddenError', message: 'custom message' }` (passes through the lib error mw unchanged per D-12).
    - Case H (auth before validation): @Authorized + invalid request body → 401 returned (NOT 400) because auth gate fires before validation per D-03.
  </behavior>
  <action>
1. Write the two test files. Use the supertest pattern from Phase 2.
2. For each case, isolate state with `beforeEach(resetContainer)`.
3. Use vi.fn() spies to count checker invocations (Case D verifies authChecker was NOT called when user is falsy).
4. Run.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/phase3/interceptor-pipeline.test.ts tests/integration/phase3/auth-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - `grep -nE "expect\(.*\)\.toBe\(401\)" tests/integration/phase3/auth-pipeline.test.ts` returns at least 3 matches (Cases A, D, H).
    - `grep -nE "expect\(.*\)\.toBe\(403\)" tests/integration/phase3/auth-pipeline.test.ts` returns at least 2 matches (Cases B, G).
    - `grep -n 'wrapped: { ctrl: true, meth: true' tests/integration/phase3/interceptor-pipeline.test.ts` returns at least 1 match (asserts D-09 order).
  </acceptance_criteria>
  <done>SC#3 and SC#4 proven across all checker outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: SC#5 user error mw + structural grep gates (ERR-04)</name>
  <read_first>
    - .planning/phases/03-middleware-interceptors-auth-error-handling/03-CONTEXT.md §D-15/D-16/D-17 (user error mw)
    - src/adapter/error-middleware.ts (libraryErrorMiddleware shape)
    - .planning/phases/03-middleware-interceptors-auth-error-handling/03-RESEARCH.md §"Pitfall 2" + §"Pattern 3"
  </read_first>
  <files>tests/integration/phase3/user-error-mw.test.ts, tests/integration/phase3/grep-gate.test.ts</files>
  <behavior>
    user-error-mw.test.ts:
    - Case A (single error mw, writes response):
      ```ts
      @Middleware({ type: 'after' })
      class UserErrMw implements ExpressErrorMiddlewareInterface {
        use(err: unknown, _req, res, _next) {
          res.status(418).json({ caught: true, name: (err as Error).name });
        }
      }
      ```
      Boot with `middlewares: [UserErrMw]`. Controller throws `new Error('boom')`. Response → 418 with body `{ caught: true, name: 'Error' }`. libraryErrorMiddleware NOT fired.
    - Case B (logger then formatter, both error mw, first calls next(err)):
      ```ts
      class LoggerErr implements ExpressErrorMiddlewareInterface {
        use(err, _req, _res, next) { logged.push(err); next(err); }
      }
      class FormatterErr implements ExpressErrorMiddlewareInterface {
        use(err, _req, res, _next) { res.status(500).json({ formatted: true }); }
      }
      ```
      Boot with `middlewares: [LoggerErr, FormatterErr]`. → response { formatted: true }; logged.length === 1.
    - Case C (logger only, calls next(err) but no formatter): logger forwards; libraryErrorMiddleware writes the standard response.
    - Case D (defaultErrorHandler === false): UserErrMw is NOT mounted (per D-17 / Phase 2 D-17), libraryErrorMiddleware also not mounted, error becomes Express's default 500 HTML.
    - Case E (err.source survives): controller throws; UserErrMw reads `(err as any).source` and includes it in the response. Asserts source === `${ControllerName}.${methodName}` (Phase 2 D-16 — Phase 3 just reads).

    grep-gate.test.ts: structural invariants encoded as runtime greps using `fs.readFileSync`:
    - Read `src/decorators/middleware.ts` — assert `/from ['"]express['"]/` matches 0 times (decorators are Express-free).
    - Read `src/decorators/middleware.ts` — assert `/Reflect\.defineMetadata/` matches 0 times.
    - Read `src/metadata/types.ts` and `src/metadata/storage.ts` — assert `/from ['"]express['"]/` matches 0 times in either.
    - Read `src/adapter/middleware.ts` and `src/adapter/interceptor.ts` — assert `/^\s*try\s*\{/m` matches 0 times (no try/catch wrapping; native v5 forwarding per D-04).
    - Read `src/index.ts` — assert exports include all six decorator names AND the three interface type names. Implementation: read file as text, then for each name in `['UseBefore', 'UseAfter', 'Middleware', 'Interceptor', 'UseInterceptor', 'Authorized', 'ExpressMiddlewareInterface', 'ExpressErrorMiddlewareInterface', 'InterceptorInterface']`, assert the file content includes that string.
    - Read `src/adapter/response.ts` — assert `next();` calls match at least 6 times (one per success branch).
    - Filter comments before counting in any of the above (to satisfy the "self-invalidating grep gate" rule from the GSD planner contract):
      ```ts
      const stripComments = (s: string) => s.split('\n').filter(line => !line.trim().startsWith('//') && !line.trim().startsWith('*')).join('\n');
      ```
  </behavior>
  <action>
1. Write user-error-mw.test.ts covering Cases A–E.
2. Write grep-gate.test.ts using `fs.readFileSync(new URL('../../../src/...', import.meta.url), 'utf8')` and Vitest assertions. Strip comment lines before regex/count assertions.
3. Run.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/phase3/user-error-mw.test.ts tests/integration/phase3/grep-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - `grep -n 'caught: true' tests/integration/phase3/user-error-mw.test.ts` returns at least 1 match.
    - `grep -n 'stripComments' tests/integration/phase3/grep-gate.test.ts` returns at least 1 match (proves the comment-stripping discipline).
    - `grep -n 'ExpressErrorMiddlewareInterface' tests/integration/phase3/grep-gate.test.ts` returns at least 1 match.
  </acceptance_criteria>
  <done>SC#5 proven; structural invariants enforced as tests.</done>
</task>

<task type="auto">
  <name>Task 4: Run full vitest suite + record final phase 3 metrics</name>
  <read_first>
    - tests/ (full directory tree — confirm count of new tests added)
  </read_first>
  <files>(none — verification + summary preparation)</files>
  <behavior>
    - Run the COMPLETE vitest suite under both `pool: 'forks'` and `pool: 'threads'` (per CLAUDE.md BUILD-08 future requirement and existing project test policy). If the project's vitest.config.ts already defines both pools, just run `pnpm vitest run`. If not, run `pnpm vitest run --pool=forks` and `pnpm vitest run --pool=threads` separately.
    - Confirm zero test failures.
    - Confirm `pnpm tsc --noEmit` exits 0.
    - Record the total test count for the SUMMARY.
  </behavior>
  <action>
1. `pnpm vitest run` — capture pass/fail count and total tests.
2. `pnpm tsc --noEmit` — confirm clean.
3. If both pool modes are configured: `pnpm vitest run --pool=forks` then `pnpm vitest run --pool=threads`. If a single config: just one run.
4. Note total Phase 3 tests added (SUM across plans 01–05). Phase 1 had 88 tests; Phase 2 had ~196. Expect Phase 3 to add roughly 60–100 tests (storage/decorator unit + adapter unit + 5 integration files).
  </action>
  <verify>
    <automated>pnpm vitest run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0; total assertion count is documented in 03-05-SUMMARY.md.
    - `pnpm tsc --noEmit` exits 0.
    - All 8 phase-3 requirement IDs (MW-01..04, AUTH-01..03, ERR-04) have at least one passing integration test that exercises them.
  </acceptance_criteria>
  <done>Phase 3 fully verified end-to-end; ROADMAP can be marked complete pending a final mark.</done>
</task>

</tasks>

<verification>
- All Phase 3 integration tests pass: `pnpm vitest run tests/integration/phase3/`.
- Full suite passes: `pnpm vitest run`.
- `pnpm tsc --noEmit` clean.
- Every Phase 3 requirement (MW-01..04, AUTH-01..03, ERR-04) maps to at least one passing test in this plan.
</verification>

<success_criteria>
- ROADMAP Phase 3 success criteria #1–#5 each verified by at least one integration test.
- MW-04 fixture (deterministic top-to-bottom ordering test) committed and passing.
- Structural grep-gate tests lock the architectural invariants from CONTEXT.md.
- Total Phase 3 test count documented in summary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-middleware-interceptors-auth-error-handling/03-05-SUMMARY.md` documenting:
- Total tests added in Phase 3 (running total across plans 01–05).
- Mapping table: each ROADMAP SC → test file proving it.
- Mapping table: each requirement ID (MW-01..04, AUTH-01..03, ERR-04) → test(s) proving it.
- Confirmation that Phase 3 is COMPLETE and ready for Phase 4 (parallel) or Phase 5 (publish).
- Note any deviations from CONTEXT.md decisions (expected: none).
</output>
